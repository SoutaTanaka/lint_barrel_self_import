/// Some test class
/// Test class for demonstration purposes.
class SomeClass {
  /// Does something for testing.
  void doSomething() {
    // Using a simple operation instead of print for testing
    const message = 'Doing something';
    assert(message.isNotEmpty, 'Message should not be empty');
  }
} 
